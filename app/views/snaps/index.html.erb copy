<% if @snaps %>
	<ul class="no-bullet" id="snaps">
    <%= render @snaps %>
  </ul>
    <%= will_paginate @snaps %>
<% else %>
	<p class='medium-6 medium-offset-3 columns panel radius text-center'>Oh, snap! No, snaps!</p>
<% end %> 
