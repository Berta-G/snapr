<li class="snap row">
	<article class='medium-6 medium-offset-3 columns panel radius text-center'>
		<%= link_to(image_tag(snap.image.url(:medium), class: 'th radius'), '/snaps/'+snap.id.to_s, id: "snap_link") %>
		<label><%= snap.description %></label>
		<% if !snap.tags.empty? %>
			<div class="row">
				<ul class="small-9 small-centered columns">
					<%= render snap.tags %>
				</ul>
			</div>
		<%end%>
	</article>
</li>
		